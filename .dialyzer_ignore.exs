# Intentionally empty.
#
# The `PhoenixKit.Settings.get_editor_mode/0` :call_to_missing entry that used
# to live here was dropped when the phoenix_kit floor moved to ~> 1.7.214, the
# release that exports the function — exactly what `list_unused_filters: true`
# in mix.exs exists to force. Keep this file (and that flag) so any future
# entry covering a not-yet-released core API is policed the same way.
[]
